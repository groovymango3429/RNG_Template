# RewardModels

Place roll outcome models here.

Naming rule: each model name must match `RollConfig.ModelName` exactly.
If a model is missing, the server warns and tries the fallback model from `AssetConfig.FallbackModelName`.
